# 复制为 .env 后替换为实际值。可使用 `openssl rand -hex 32` 生成密钥。
DATABASE_URL=postgresql://esports_user:replace_me@localhost:5432/esports_db
SECRET_KEY=

# 仅首次创建 admin 用户时填写至少 12 位随机密码；创建完成后应从 .env 删除。
INITIAL_ADMIN_PASSWORD=

PROJECT_NAME=电竞馆智能选址系统
API_V1_STR=/api/v1
BACKEND_CORS_ORIGINS=[]
